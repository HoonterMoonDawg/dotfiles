# Autocomplete options
#zstyle ':autocomplete:list-choices:*' min-input 3
# Enable completion caching, use rehash to clear
zstyle ':completion:*' use-cache on
zstyle ':completion:*' cache-path $zshd/.zshcache
